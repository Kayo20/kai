# Copy this file to `.env` and fill in real values. DO NOT commit your `.env` file.

# Mail (SMTP) settings used by the contact form
MAIL_HOST=your-smtp-host
MAIL_PORT=587
MAIL_USERNAME=your-smtp-username
MAIL_PASSWORD=your-smtp-password
MAIL_FROM="Kai <no-reply@yourdomain.com>"
CONTACT_RECEIVER=yourteam@yourdomain.com

# Eventbrite API credentials
EVENTBRITE_CLIENT=your_eventbrite_client_id
EVENTBRITE_SECRET=your_eventbrite_client_secret

# Wise API token (used to construct checkout link)
WISE_API_TOKEN=your_wise_api_token

# Microsoft Bookings URL (open in new tab). Example:
# https://outlook.office.com/book/your-org/
MS_BOOKINGS_URL=https://outlook.office.com/book/your-booking-url

# Optional reCAPTCHA keys (only if you re-enable captcha)
VITE_RECAPTCHA_SITE=your_recaptcha_site_key
RECAPTCHA_SECRET=your_recaptcha_secret

# Notes:
# - For local development: copy this file to `.env` in the project root and fill values.
# - Never commit your `.env` file. Add `.env` to `.gitignore` if not already ignored.
# - When deploying to Netlify: add these variables in Site Settings → Build & deploy → Environment → Environment variables.
